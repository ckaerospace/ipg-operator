services:
  - type: web
    name: ipg-operator
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API
        value: https://ipg-cea-api.onrender.com
